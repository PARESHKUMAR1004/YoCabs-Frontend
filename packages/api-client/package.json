{
  "name": "@yocabs/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed client for the YoCabs REST API, shared by the mobile app and the admin web.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --exclude test/e2e/**",
    "test:e2e": "vitest run test/e2e"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "vitest": "^3.2.4"
  }
}
