# Dev only: where the Vite dev server proxies /api requests.
YOCABS_API_URL=http://localhost:8080

# Production builds: absolute API origin (leave empty when the console is served from the same origin as the API).
VITE_API_BASE_URL=
